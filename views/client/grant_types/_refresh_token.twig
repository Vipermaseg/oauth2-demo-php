<p>
    El <code>Token de refresco</code> se suele usar en tandem con el tipo de concesión de <code>Código de autorización</code>. Haz click en "Autorizar" para obtener un Código de autorización:
</p>
<a class="button" href="{{ app.parameters.authorize_route|slice(0, 4) == 'http' ? app.parameters.authorize_route : url(app.parameters.authorize_route) }}?response_type=code&client_id={{app.parameters.client_id}}&redirect_uri={{ url('authorize_redirect', {show_refresh_token:1})|url_encode() }}&state={{session_id}}">Autorizar</a>
